import { CTSearch } from 'src/types';

export class CTSearchService {
  async getCTSearchs(): Promise<CTSearch> {
    return null;
  }
  async createCTSearch(): Promise<CTSearch> {
    return null;
  }
}
